# Copy this file to `.env` and replace the values if needed
# cp .env.example .env

# --- Database (Postgres) ---
DB_NAME=resurrection_db
DB_USERNAME=postgres
DB_PASSWORD=password

# --- Security (JWT) ---
# Must be at least 32 characters long
JWT_SECRET_KEY=example_secret_key_for_local_development_only_123456